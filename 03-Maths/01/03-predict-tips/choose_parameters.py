# pylint: disable=missing-docstring

from compute_error import *

def select_best_parameters(slopes, intercepts):
    '''
        TO DO: return a dictionnary containing the slope, the intercept and the mean_error corresponding to the best fit (min error)
        => {"mean_error":..., "slope":.., "intercept":...}
    '''
    pass
